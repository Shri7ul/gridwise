# 2026-09-18 — GridWise (BUP CSE Fest 2026, Preliminary)

## What was built

Complete submission for the *GridWise — Smart Campus Energy Optimizer* preliminary round.
7-phase plan from the user, all phases delivered and tested.

Final tree (workspace root `D:\Hackathon\BUP`):

```
.env.example  .gitignore  Dockerfile  README.md  render.yaml  requirements.txt
app/config.py        env-driven Settings + lru_cache + redact()
app/models.py        strict Pydantic v2 (extra="forbid"), HourIndex/Finite* aliases
app/llm_interpreter.py  prompt + extract_json_payload + repair + retry loop
app/guardrails.py    validate_interpretation + EffectiveConstraints + feasibility_warnings
app/optimizer.py     PuLP/CBC LP + post-solve netting + _assert_plan_valid replay gate
app/summary.py       deterministic plan summary (NOT the LLM)
app/fallback.py      opt-in degraded interpreter (default off)
app/main.py          FastAPI, /health + /optimize-energy, 400/422/500 taxonomy
tests/run_public_samples.py  judge-parity harness (--offline | --base-url)
tests/test_llm_stage.py      14 unit tests, stub provider
tests/test_api_e2e.py        12 tests, TestClient
```

## Key decisions

- **solar_reduction `factor` is the usable fraction REMAINING** (80% reduction → 0.2),
  not the reduction amount. Confirmed against public sample ground truth.
- **Whole-hour windows are start-inclusive, end-exclusive** (1 PM→3 PM = [13,14]).
- **End-of-day neutrality fixes total grid energy** at `sum(demand) − sum(solar_used)`.
  The LP is therefore *degenerate* — many distinct schedules share the optimal cost.
  Judge scores validity + cost, not the action sequence. Our plans match the published
  references to the exact BDT (ratio 1.0000).
- Post-solve `_assert_plan_valid()` replays the returned plan through every constraint.
  If the LP says X but the plan says Y, we fail loudly instead of shipping a bad plan.
- `summary.py` is deliberately deterministic — using the LLM for the summary as well
  would violate the challenge's intent.

## Measured results

- Public samples: **10/10 passed**, quality ratio mean 1.0000 / min 1.0000
- LLM stage unit tests: 14/14
- API e2e tests: 12/12
- Latency p95 ≈ 0.13–0.15 s offline, ≈ 0.45 s live

**Correct invocation for the sample runner** (found the hard way — `--offline` runs the
*real* interpretation stage, so it needs a credential or the fallback flag):

```bash
ALLOW_DETERMINISTIC_FALLBACK=true python tests/run_public_samples.py --offline
# or, with a key:  GROQ_API_KEY=... python tests/run_public_samples.py --offline
```

Without either, all 10 cases return a controlled 422 — correct behaviour, not a bug.
Documented in the runner docstring and README so nobody re-debugs it.

## Environment gotchas (Windows)

- `HTTP_PROXY=http://127.0.0.1:59085` is set in this shell and breaks localhost calls —
  use `--noproxy '*'` / `NO_PROXY` when hitting 127.0.0.1.
- Background servers die when their spawning shell exits. Start the server and run the
  tests in a **single** Bash invocation.
- `pulp.__version__` reports 3.0.2 even on dist 3.2.2 (stale attribute). Harmless — do
  not "fix" it.
- `pulp==3.1.0` does not exist on PyPI; the pinned version is 3.2.2.
- Docker unavailable here, so the image was never built. The CBC solver ships inside the
  pulp wheel (`cbc.exe`) — verified present, so no system `coinor-cbc` is needed.

## Not done (user action required)

1. Supply a real LLM key (GROQ or OPENAI) to exercise the live interpreter path —
   all tests ran against a stub or the opt-in fallback.
2. Build + deploy to Render Free Tier.
3. Record the 3-minute demo video (not a code task).
4. Flip the repo public after the deadline.

## Spec audit (both PDFs + sample JSON, read line by line)

Verdict: implementation is **spec-faithful**. Every mandate in Problem Statement §04–§11
and Guide §02/§07/§08/§10 is implemented. Nothing found that would cost points.

Confirmed against spec, no action needed:
- Request schema = §07 exactly; response schema = §10 exactly (7 fields, 6+5 sub-fields,
  no extras, no omissions). `extra="forbid"` is consistent with "structurally invalid
  request" ⇒ 400.
- HTTP codes: spec defines only **200/400/422/500** — no 413 exists, so the middleware's
  400 for an oversized body is spec-compatible (not a defect).
- All 6 directive types, their exact `structured_adjustment` key sets, `applies`
  semantics, hours convention, factor-as-remaining, reserve folding via `max()` with the
  base minimum, grid-cap folding via `min()`, no-charge/no-discharge — all match §5.3/§08.
- Penalty table replayed: balance, bounds, transitions, rate limits, effective solar,
  directive compliance, neutrality, totals-vs-plan. `_assert_plan_valid()` enforces the
  identical set server-side; the harness enforces it again client-side.
- `/health` returns `{"status":"ok"}`; latency p95 ≈0.13–0.15 s offline vs 5 s for full
  marks; 30 s per-request timeout honoured via `request_deadline_seconds` budget.

The one thing to be careful about (Guide §09, "Required LLM absent from the
operator-note interpretation path … not eligible for the final preliminary shortlist"):
`ALLOW_DETERMINISTIC_FALLBACK` defaults to **false**, `app/llm_interpreter.py` is the
always-primary path, and `render.yaml` pins the flag to `false`. README line 157 explains
the LLM is the only text→directive component and 525 documents the fallback as opt-in and
labelled. Reviewed and judged acceptable — but **do not set the flag on the hosted
instance**. If it is ever on during judging, a keyword interpreter is the sole path and
Guide §09 applies.

## Bugfix: Swagger UI showed "No parameters" with no input box

Reported by the user: opening `/docs`, `POST /optimize-energy` rendered **"No parameters"**
and no request-body box, so the endpoint was unusable from the browser.

**Root cause.** The route does `raw = await request.body()` itself, so it can map malformed
JSON to 400 instead of letting FastAPI's parser raise a 422 traceback. That raw-body read
makes the body **invisible to FastAPI's OpenAPI inference** — nothing to declare a body
from, so Swagger shows no editor. The runtime taxonomy was correct; only the docs were broken.

**Fix** (`app/main.py`): kept the raw-body parsing untouched, added `openapi_extra=
{"requestBody": {...}}` to the decorator with `schema=OptimizationRequest.model_json_schema()`
and `example=_sample_body()`. `_sample_body()` builds a valid 24-hour scenario (the exact
§07.4 example notes) and is now the single source for both Swagger and any future doc.

**Regression tests added** (`tests/test_api_e2e.py`, 12 → 15 tests):
- `test_openapi_declares_request_body_for_swagger` — requestBody present + required + has
  schema + has example. Guards against the box silently disappearing again.
- `test_openapi_example_is_a_valid_24_hour_scenario` — the example must satisfy
  `OptimizationRequest` outright; a pre-filled example that 422s is worse than none.
- `test_documented_example_round_trips_through_the_endpoint` — POSTs the example with
  `_post` stubbed to the ground-truth reading; asserts 200, 24 hours, and that
  `no_charge_window` actually reaches the schedule.

**Lesson worth remembering:** any FastAPI route that reads the raw body must declare
`openapi_extra.requestBody` or its Swagger UI is dead. My first version of the round-trip
test failed because it hit the live provider instead of the per-test `_post` stub — follow
the existing `_stub_post_factory` pattern in that suite.

**Verified:** e2e 15/15, LLM stage 14/14, samples 10/10 (ratio 1.0000); a real uvicorn
instance POSTed the documented example over HTTP and returned 200 with
`solar_reduction / no_charge_window / no_op`. README gained an "Interactive docs
(Swagger UI)" subsection.

## Two real bugs found once a live credential was available

The user dropped a real `GROQ_API_KEY` into `.env` mid-session, which finally exposed
problems that every stubbed test had been hiding.

### Bug A — `.env` was never loaded (documented quickstart was broken)

`app/config.py` read config with `os.getenv()` only. `python-dotenv` was pinned in
`requirements.txt` but **nothing imported it**, so a correctly-filled `.env` was silently
ignored and every request failed with "no LLM credential configured". The README even told
users to `export $(grep -v '^#' .env | xargs)` as a workaround — evidence the file was
known not to load.

Fix: added `_load_dotenv_once()`, called at import, resolving `.env` next to the project
root via `Path(__file__).resolve().parent.parent`. Critically it passes
**`override=False`** so a platform-injected variable (Docker `-e`, Render env) beats the
file — otherwise a committed `.env` could shadow a deployment secret. Import is guarded so
a missing optional dep degrades to "no .env support" rather than crashing.

### Bug B — the default model no longer exists on Groq

`llama-3.3-70b-versatile` returns `404 model_not_found`. Groq's current lineup (verified via
`GET /models`) is `openai/gpt-oss-120b`, `qwen/qwen3.8-27b`, `openai/gpt-oss-20b`,
`groq/compound`, `allam-2-7b`, plus whisper/prompt-guard models. No Llama model at all.

Fix: default is now `openai/gpt-oss-120b` in `config.py`, `.env.example`, `render.yaml`.
All three models were tested live and produce **exactly correct** first-attempt
interpretations. `qwen/qwen3.8-27b` is fastest (~0.9 s vs ~1.3 s).

**Groq retires models regularly — always verify before deploying:**
```bash
curl -s https://api.groq.com/openai/v1/models -H "Authorization: Bearer $GROQ_API_KEY"
```

### Quota, not a bug: 8000 tokens/minute

10 live cases back-to-back gives 6/10 — the rest fail with a controlled 422/500. Not an
interpretation failure: the free tier allows **8000 tokens/minute** and each request costs
~1500–2500 tokens (the system prompt documents all six directive types), so the bucket
empties after ~5 requests. Proven by re-running a "failed" case after a 65 s cooldown.

Fix: added `--delay SECONDS` to `run_public_samples.py`. `--delay 15` gives 10/10 live.
This is a provider-account limit, not an implementation defect.

### SECURITY — the live key was sitting in `.env.example`

`.gitignore` has `!.env.example`, so the template **is** committed while `.env` is not. The
real 56-char key had been pasted into the template at line 8 — it would have been published
to a public repo after the deadline, violating the Guide's secret rule.

Fixed (emptied to placeholders) and guarded by two new tests in `tests/test_config.py`:
- `test_no_committed_file_contains_a_credential` — walks the whole repo for credential-shaped
  literals (gsk_, sk-, sk-proj-, AIza, hf_). Excludes obviously synthetic fixtures
  (SUPERSECRET/EXAMPLE/DUMMY/…) so the redaction test's fake key does not trip it — a linter
  that flags its own fixtures trains people to ignore it. **Negative-controlled:** planting a
  realistic key fails the suite, removing it passes.
- `test_env_example_ships_only_placeholders`.

## Test suite now 49 tests

| Suite | Count |
|---|---|
| `tests/test_config.py` (new) | 10 |
| `tests/test_llm_stage.py` | 14 |
| `tests/test_api_e2e.py` | 15 |
| `tests/run_public_samples.py` | 10 cases |

Final verified run (live Groq, `openai/gpt-oss-120b`): config 10/10, llm stage 14/14,
e2e 15/15, samples **10/10 live** with quality ratio 1.0000 and p95 2.99 s.

**Lesson:** stub-everything test suites cannot catch a broken credential path. The `.env`
bug and the retired-model bug were both invisible to 41 green tests and only appeared the
moment one real key existed. When a project's docs say "copy this file and fill it in",
there must be a test that proves the file is actually read.

## Fresh-clone verification (the judge's actual path)

Simulated a real clone (everything except `.env`, `__pycache__`, scratch) into a clean
directory and followed the README verbatim. **Result: fully working.**

- `cp .env.example .env` with no credential → `provider=unconfigured`, `/health` returns
  `{"status":"ok"}`, clear startup warning. No crash. Correct.
- Credential injected as a **platform env var** (Render/Docker path) → 10/10 live samples,
  quality ratio 1.0000 on every case, p95 3.30 s.
- All suites green in the clone: config 10/10, llm stage 14/14, e2e 15/15.

**Proved env precedence with a conflicting value** (this is the property that matters for
deployment): put `LLM_MODEL=stale-value-from-dotenv-file` in the clone's `.env`, then
- with `LLM_MODEL` also set in the environment → resolved `openai/gpt-oss-120b` (platform wins)
- without it → resolved `stale-value-from-dotenv-file` (`.env` wins locally)

So a committed `.env` can never shadow a deployment secret. Verified, not assumed.

### Fixed a misleading log label

The startup line printed `port=8000` while uvicorn was actually bound to whatever `--port`
said (e.g. 8021). `settings.port` is the *configured* `$PORT`, not the bound port. Renamed
the field to `configured_port=` so it is not read as a lie during local debugging. Behaviour
unchanged — Render/Docker both drive the port through `$PORT`, so the value is correct there.

### Added .dockerignore (defence in depth)

I initially suspected a secret-baking bug, then **checked before claiming it**: the
Dockerfile uses explicit `COPY app ./app` etc. rather than `COPY . .`, so `.env` is never
copied into the image and there was no actual leak. Added `.dockerignore` anyway to keep the
build context small and guarantee a future `COPY . .` cannot bake in a credential. Verified
no legitimate path (`app/`, `tests/`, `pdf/`, `README.md`, `requirements.txt`) is matched by
any exclusion pattern.

### Dockerfile statically verified (daemon still unavailable)

Cannot build the image here, so verified what can be checked without one:
- every COPY source path exists in the context
- `libgomp1` (CBC dependency) and `curl` (needed by HEALTHCHECK) are both installed in the
  runtime stage **before** they are used
- binds `0.0.0.0`, honours `$PORT`, `EXPOSE 8000`, non-root user, no baked secrets
- README documents the pullable reference, exact tag, env var names, port, and one run cmd

Still requires the user to actually build/push — `docker` is not on PATH here.

## Docker bug found and fixed: wrong apt package for the CBC solver

While statically verifying the Dockerfile I read the CBC binary's `DT_NEEDED` entries
directly. The Dockerfile installed **`libgomp1`** "because CBC needs OpenMP". That is
false:

```
$ strings pulp/solverdir/cbc/linux/i64/cbc | grep -E '^lib(stdc\+\+|gomp)'
libgcc_s.so.1
libstdc++.so.6        # <-- never libgomp
```

The real dependency is **`libstdc++6`**, and `python:3.12-slim` does **not** ship it.
Confirmed from the authoritative build source
(`docker-library/python/3.12/slim-bookworm/Dockerfile`): the runtime stage installs only
`ca-certificates`, `netbase`, `tzdata`, then runs `apt-get purge -y --auto-remove`, which
removes the `g++` toolchain that would have provided libstdc++.

**Impact:** the solver would fail to start inside the image. Since the Guide designates the
Docker image as the fallback path when the hosted endpoint is unavailable, this would have
lost both the fallback and its 4 rubric points — and it would only have shown up at
container runtime, never in local testing (Windows CBC is a different binary with different
deps).

**Fix:** `apt-get install libstdc++6 curl` (libstdc++6 pulls libgcc-s1 automatically).
Also corrected the wrong claim in the README, which repeated the libgomp1 story.

**New suite `tests/test_docker_contract.py` (9 tests)** — machine-checks what is verifiable
without a daemon: CBC exists in the wheel, every non-glibc library CBC needs is provided by
an installed package (with `TRANSITIVE_PACKAGES` so libgcc-s1 is accepted as a dependency of
libstdc++6), COPY sources exist, binds 0.0.0.0, honours `$PORT`, non-root, and no
build-arg/COPY secret leakage.

**Negative-controlled:** reverting the Dockerfile to `libgomp1` fails 2 tests; restoring
`libstdc++6` gives 9/9. Re-verify after a pulp upgrade with:
```bash
strings <pulp>/solverdir/cbc/linux/i64/cbc | grep -E '^lib(stdc\+\+|gomp)'
```

**Lesson:** "never tested" is not "fine". Three separate bugs in this project were found by
checking a claim the code merely asserted — the `.env` not loading, the retired model, and
now the wrong solver library. Each had a confident comment next to it explaining behaviour
that was not real.

## Test suite now 58 tests

| Suite | Count |
|---|---|
| `tests/test_docker_contract.py` (new) | 9 |
| `tests/test_config.py` | 10 |
| `tests/test_llm_stage.py` | 14 |
| `tests/test_api_e2e.py` | 15 |
| `tests/run_public_samples.py` | 10 cases |

All green: 9 + 10 + 14 + 15 = 48, plus the 10-case live harness at ratio 1.0000.






---

## Render deployment guide (`DEPLOY_RENDER.md`)

Answered "how to deploy it on render" with an 8-step runbook, then verified
every command in it rather than writing instructions and hoping.

**Verified before writing:**
- `pulp-3.2.2-py3-none-any.whl` is `py3-none-any` → pure Python, no compile on
  Render; CBC ships inside the wheel, so the "no `apt-get`" limit is a non-issue.
- `.env` is gitignored; `.env.example` is committed (the `!.env.example` rule).
- Simulated Render's env (`env -u LLM_MODEL ... PORT=10000`): model, base_url,
  port, fallback flag and key resolution all correct.
- Startup line captured verbatim:
  `gridwise starting: version=1.0.0 provider=groq model=openai/gpt-oss-120b llm_configured=True fallback_enabled=False configured_port=10000`
- Live HTTP on `0.0.0.0:10000` with the real key: `/health` → 200;
  `/openapi.json` declares `requestBody` with `required:true` and a 24-hour example.
- README's verification command, run verbatim: **10/10, ratio 1.0000, p95 2.43s**.

**Two doc corrections found by executing, not reading:**
1. Response field is **`directive_type`**, not `type`; summary key is
   **`plan_summary`**, not `summary`. The README was already right; only my
   first inspection used the wrong key and printed `None`.
2. `total_grid_kwh` for the guide's example payload is **4071.0** (I first wrote
   4055.0 from memory). Now stated with the invariant to check instead:
   `total_grid_kwh == sum(hourly_plan[*].grid_kwh)` and final battery energy
   returns to 200.0. Cost/totals are payload-specific — flagged as such.

**Windows curl trap (documented in the guide):** `-d "$BODY"` silently sent an
empty body → `400 {"error":"request body is empty"}`. `--data-binary @file` is
the verified path. This was my own quoting bug, not a service defect — worth
noting because it looks exactly like an API failure.

**Also:** `pytest` was missing from the isolated venv; installed it (9.1.1).
Test suite unchanged at 48 + 10-case harness, all green.

**README now links the guide** from the TOC and the "Deploy on Render" section,
and documents `--delay 15` (8000 tokens/min free tier exhausts after ~5 cases).
